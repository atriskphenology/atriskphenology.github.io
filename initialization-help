##bash code for getting website going ##
#start command prompt with Ruby#
ridk enable

#choco install sass something else had to insall too

pwd #shows what current directory is#
gem install bundler
gem install jekyll
gem install github-pages
gem install just-the-docs
gem install rails
gem install optimist
gem install sassc
gem install websocket
gem install styleinct


#now that yoy have files, we can go to the webpage files in Git Bash

cd C:/Users/Kelse/Documents/atriskphenology.github.io

#bundle init
#init above for the first time in new folder
bundle update
#generates Gemfile#

bundle exec just-the-docs rake search:init
#generate search files

bundle exec jekyll serve

bundle exec jekyll clean
#will clean out the files from another template

bundle exec jekyll build
#will build the website for the publishing

ssh-keygen -t ed25519 -C "kelsey.king@wsu.edu"

Your identification has been saved in /c/Users/Kelse/.ssh/id_ed25519
Your public key has been saved in /c/Users/Kelse/.ssh/id_ed25519.pub
The key fingerprint is:
SHA256:Rs7cnPAVcks6Ie9f+ak/KiOfYhauXoANxsIgYXatKGA kelsey.king@wsu.edu
The key's randomart image is:
+--[ED25519 256]--+
|o= ..   . o +    |
|=E+ ..   o * o   |
|o .o.+  o + o    |
|.. .o += * +   . |
|.    . oS *   o  |
|       ... . . ..|
|        ... .  ..|
|        .* o. .. |
|      .o+ +oooo..|
+----[SHA256]-----+
